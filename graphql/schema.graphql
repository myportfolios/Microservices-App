type Query {
  books: [Book]
}
type Mutation {
  createBook(input: CreateBookInput!): Book
}
type Book {
  title: String
  author: String
  numberPages: String
  publisher: String
}
input CreateBookInput {
  title: String
  author: String
  numberPages: String
  publisher: String
}
